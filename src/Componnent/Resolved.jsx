
const Resolved = ({item}) => {
    return (
        <div>
        <h1 className="text-xl space-y-5 mt-3 py-3 px-3 rounded-lg font-semibold bg-blue-100">{item.title}</h1>
        </div>
    );
};

export default Resolved;